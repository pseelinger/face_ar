# -*- coding: utf-8 -*-
db = DAL("sqlite://storage.sqlite")

db.define_table('faces',
                Field('user_id', 'string'),
                Field('pic_num', 'integer'),
                Field('file', 'upload'),
                format = '%(user_id)s')

# rows = db(db.faces.id > 0).select(limitby=(1, 2), orderby=db.faces.id)
# rows.first().delete_record()
